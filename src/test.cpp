#include <iostream>
#include <vector>
#include <sstream>

struct Account {
    int number;
    std::string name_first;
    std::string name_middle;
    std::string name_last;
    int cvc;
    std::string expiry;
    double balance_loan;
    double balance_savings;
    std::string date_created;
};

int WinMain(int argc, char const *argv[])
{
    return 0;
}